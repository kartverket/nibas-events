spring:
  flyway:
    enabled: false
  cloud:
    vault:
      enabled: false
    gcp:
      pubsub:
        enabled: false
      core:
        enabled: false

  r2dbc:
    username: user
    password: password
    url: r2dbc:postgresql://localhost:5433/nibas
