name: nibas-promote-to-prod
on: workflow_dispatch

jobs:
  deploy-argo:
    uses: kartverket/smia-apps/.github/workflows/promote-app.yml@main
    with:
      application: nibas-events
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_ARGOCD }}
